name: Check PR text

on: [pull_request]

jobs:
  check_pr:
    runs-on: ubuntu-latest
    name: Check internal links
    steps:
    - name: Scan for research.protocol.ai occurrences
      uses: jsoares/github-pr-contains-action@9131b2b
      with:
        diffDoesNotContain: 'research.protocol.ai'
